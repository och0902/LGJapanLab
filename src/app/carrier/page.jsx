'use client';

import React from 'react';
import { Box } from '@mui/material';


const Carrier = () => {
   return (
      <Box sx={{ my: '10vh', display: 'flex', flexDirection: 'column', gap: '15px', m: 'auto', }} > 
         Carrier
      </Box>
   );
};


export default Carrier;